import java.util.HashSet;

public class FindTwoIndex {

    private int targetSum;

    public static int[] findTwoSum(int[] nums, int targetSum) {
        HashSet<Integer> hashSet = new HashSet<>();

        int[] index = new int[2];

        for (int i = 0; i < 1; i++) {
            int complement = targetSum - nums[i]; // Вычисляем необходимое для достижения суммы
        }
        return index;
    }
}
